import React from "react";
import { default as AppBar } from "../Components/AppBar";

const Calendar = () => {
  return (
    <div>
      <div>
        <AppBar />
      </div>
      MY CALENDAR
    </div>
  );
};

export default Calendar;
